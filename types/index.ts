export interface TripData {
    destination: string;
    startDate: string;
    endDate: string;
    tripType: string;
    style: string;
}
export interface WeatherData {
    cityName: string;
    temperature: number;
    condition: string;
    description: string;
    icon: string;
}